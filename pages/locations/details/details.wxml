<!--pages/locations/details/details.wxml-->
<view class='content-page'>
  <view class='title-area'>
    <view class='title'>{{title}}</view>
    <view class='address'>{{address}}</view>
    <view class='tags'>
      <text  wx:for="{{tag}}">{{item}}</text>
    </view>
  </view>
  <view class='show-img-area'>
    <x-scroll-banner></x-scroll-banner>
  </view>
  <view class='content'>
    <text class='subtitle'>交通路线</text>
    <text class='address'>{{traffic}}</text>
  </view>
  <view class='content'>
    <text class='subtitle'>校区认证</text>
    <scroll-view class="" scroll-x style="width: 100%">
      <view class='school-detail'>
        <view class='list' wx:for="{{schoolTags}}">
          <image  class='school-img' src='{{item.img}}'></image>
          <text class='name'>{{item.name}}</text>
        </view>
      </view>
    </scroll-view>
  </view>
  <view class='content'>
    <facility></facility>
  </view>
  
  <!-- <view class='content'>
    <text class='subtitle'>校区设施</text>

    <view class="content-row">
      <block wx:for="{{facilities}}" wx:for-index="idx" wx:for-value="item">
        <block wx:if="{{idx < 4}}">
          <view class="c-loc-facility-item content-item">
            <view class="content-item">
              <view class="content-align-center" >
                <image src="{{item.image}}"></image>
              </view>
              <text>{{item.name}}</text> 
            </view>
          </view>
        </block>				
			</block>
	
			<block wx:if="{{facilities.length > 4}}">
        <view class="c-loc-facility-item content-item">
					<view class="content-item">
						<view class="content-align-center">
							<text class=".c-loc-facility-more-num">+{{facilities.length - 3}}</text> 
						</view>
            <text class=".c-loc-facility-more">更多</text> 
          </view>
        </view>
      </block>
		</view>
  </view> -->
</view>